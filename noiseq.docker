FROM bioconductor/bioconductor_docker:RELEASE_3_14

# Main package:
RUN R -e 'BiocManager::install("NOISeq")'

# Auxiliary packages (data.table is good for reading in big expression files):
RUN R -e 'install.packages("data.table")'
